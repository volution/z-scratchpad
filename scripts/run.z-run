#!/dev/null




::// run / ...
::// watch / run / ...




++<< run / examples / server
	test "${#}" -eq 0
	exec -- "${ZRUN[@]}" ':: run / exec' \
			./examples \
	#
!!

:: watch / run / examples / server / watch :: exec -- "${ZRUN[@]}" ':: sources / watch' ':: run / examples / server' "${@}"




:: run / version :: exec -- "${ZRUN[@]}" ':: run / exec' --version "${@}"




--<< run / exec
	exec -- "${ZRUN[@]}" ':: go / execute / main / debug' "${@}"
!!

